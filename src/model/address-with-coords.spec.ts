import { AddressWithCoords } from './address-with-coords';

describe('AddressWithCoords', () => {
  it('should create an instance', () => {
    expect(new AddressWithCoords()).toBeTruthy();
  });
});
