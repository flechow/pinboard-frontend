import { PasswordChangeRequestBody } from './password-change-request-body';

describe('PasswordChangeRequestBody', () => {
  it('should create an instance', () => {
    expect(new PasswordChangeRequestBody()).toBeTruthy();
  });
});
