import { LoginUserRequestBody } from './login-user-request-body';

describe('LoginUserRequestBody', () => {
  it('should create an instance', () => {
    expect(new LoginUserRequestBody()).toBeTruthy();
  });
});
