import { EmailChangeRequestBody } from './email-change-request-body';

describe('EmailChangeRequestBody', () => {
  it('should create an instance', () => {
    expect(new EmailChangeRequestBody()).toBeTruthy();
  });
});
