import { RegisterUserRequestBody } from './register-user-request-body';

describe('RegisterUserRequestBody', () => {
  it('should create an instance', () => {
    expect(new RegisterUserRequestBody()).toBeTruthy();
  });
});
