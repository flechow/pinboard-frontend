import { GetMessagesRequestBody } from './get-messages-request-body';

describe('GetMessagesRequestBody', () => {
  it('should create an instance', () => {
    expect(new GetMessagesRequestBody()).toBeTruthy();
  });
});
